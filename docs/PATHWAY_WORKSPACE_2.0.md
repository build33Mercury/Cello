# Cello 2.0 Pathway Workspace

The v2 pathway workspace combines a compact model-graph schematic, process table, model-state table and live flux history. Arrow labels show represented processes and current model flux.

The schematic is a model communication layer rather than a comprehensive pathway database.
