# Cello 3.1 Pathway Workflow

v3.1 adds coverage filtering, cell-specific filtering, persistent pinned pathways, search focus, graph export, and graceful plotting-backend fallback.

These are navigation, presentation and export features. They do not change pathway definitions or kinetic equations.
