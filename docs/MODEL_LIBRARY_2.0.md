# Cello 2.0 Cell Model Library

The Cell Model Library exposes the eight built-in model specifications and supports specification export.

Reproducible presets contain a built-in base-model ID plus explicit parameter and environment overrides. Importing a preset does not install arbitrary kinetic code.
