# Cello v1.4.0

Release theme: Perturbation Lab.

## Rebuilt Windows asset

`Cello.exe`

SHA-256:

```text
4a96041f529731c6784a8429c8fb79d8ab39dd1792190df8ff18b50702a55ce2
```

The release adds control-matched perturbation workflows, chemical perturbation design, enzyme-activity scaling, protocol composition, live effect manifests, and protocol export.

The original multi-file startup path was replaced by the single-file embedded-payload bootstrap after the older launcher proved unreliable. Build-time structure and payload-integrity checks pass. Native Windows GUI smoke testing is still required before host-level verification.
