# Cello v1.3.0

Release theme: Dynamic Pathway Explorer.

## Rebuilt Windows asset

`Cello.exe`

SHA-256:

```text
abf4fab534894251922d5676c39f478ce68787fabe10155848564facf88244d6
```

The release adds interactive pathway views, live flux/state inspection, reaction/process details, inhibition and compartment cues, 3D process focusing, and pathway snapshot export.

The original multi-file startup path was replaced by the single-file embedded-payload bootstrap after the older launcher proved unreliable. Build-time structure and payload-integrity checks pass. Native Windows GUI smoke testing is still required before host-level verification.
