# Cello v1.5.0

Release theme: Quantitative Analysis.

## Rebuilt Windows asset

`Cello.exe`

SHA-256:

```text
ea99b67325c8638549f73aaf51e6b6332e0cae8c4be3356e7adeb0aca5763ca6
```

The release adds time-series, distribution, scatter, correlation, and run-comparison views; descriptive statistics; fold/percent change; AUC; peak/trough detection; stability diagnostics; and publication-oriented data/figure export.

The original multi-file startup path was replaced by the single-file embedded-payload bootstrap after the older launcher proved unreliable. Build-time structure and payload-integrity checks pass. Native Windows GUI smoke testing is still required before host-level verification.
